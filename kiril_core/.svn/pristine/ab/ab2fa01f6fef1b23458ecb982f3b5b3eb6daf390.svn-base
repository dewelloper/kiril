namespace Markum.Cloud.Libraries.Mail
{
    // API'den dönen sonuç değerleri
    // Functions return a value of 1 for success and 0 for failure. Other status codes may be returned as information on errors
    // ListMailbox fonksiyonu = Returns 0 if failure, 1 if complete, 2 if partial return
    public static class MEResult
    {
        public const long SUCCESS = 1L;
        public const long ERROR = 0L;
        public const long PARTIAL = 2L;
    }
}